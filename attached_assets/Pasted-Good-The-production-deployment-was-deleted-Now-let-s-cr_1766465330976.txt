Good! The production deployment was deleted. Now let's **create a fresh deployment** that will use the correct database credentials.

## Step-by-Step: Create New Production Deployment

### 1. **Verify Your Secrets Tab is Correct**
Go to your Replit project → Click **"Secrets"** (🔒 lock icon in left sidebar)

Make sure it shows:
```
PGHOST = ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech
DATABASE_URL = postgresql://neondb_owner:npg_ExO7H6bMTzSJ@ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require
```

✅ If correct, proceed to step 2.

---

### 2. **Create a NEW Production Deployment**

**Option A: Using Replit UI**
1. Click **"Deployments"** in the left sidebar
2. Click **"Create Deployment"** or **"Deploy"** button
3. Select **"Production"** deployment type
4. Click **"Deploy"**
5. **Wait 3-5 minutes** for it to build and start

**Option B: Using Replit AI Agent**

Tell the Replit AI Agent:
```
Create a new production deployment. Make sure it uses the environment variables from the Secrets tab:
- DATABASE_URL: postgresql://neondb_owner:npg_ExO7H6bMTzSJ@ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require
- PGHOST: ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech

Deploy to production now.
```

---

### 3. **After Deployment Completes - Test It**

Wait for deployment to finish, then test:
```
https://whatsapp-watch-parser-v-2.replit.app/api/db-check
```

**Expected result (SUCCESS):**
```json
{
  "status": "connected",
  "pghost": "ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech",
  "listingCount": "6749056"
}
```

---

## If It STILL Shows the Old Database (ep-floral-tree)

This means the database URL might be **hardcoded somewhere in your code**. Let's check:

### Ask Replit AI Agent to Search:

```
Search the entire codebase for "ep-floral-tree" and show me every file that contains it. 
Also search for any file that contains "aws.neon.tech" and show me all occurrences.
```

If it finds any files with the old database endpoint, we need to remove those hardcoded values.

---

## Most Likely Fix: .env File Issue

Your project might have a `.env` file with old credentials. 

**Tell Replit AI Agent:**
```
1. Check if there is a .env file in the project root
2. If it exists, delete it or update it with:
   DATABASE_URL=postgresql://neondb_owner:npg_ExO7H6bMTzSJ@ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require
   PGHOST=ep-royal-poetry-ae4cqfti.c-2.us-east-2.aws.neon.tech
3. Redeploy to production
```

---

## Quick Checklist:

✅ Secrets tab has correct `ep-royal-poetry` database  
✅ Old production deployment deleted  
⬜ New production deployment created  
⬜ Test URL shows "ep-royal-poetry" and "connected"  

Try creating the new deployment first. If it still fails, come back and we'll search for hardcoded database URLs in your code.

What happens when you try to create a new deployment? Does it complete successfully?